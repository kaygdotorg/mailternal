// Wave-0 stub — replaced by chunk A (AppShell).
import SwiftUI
import MailternalInterfaces

@main
struct MailternalApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Mailternal 0.0.1 scaffold")
                .frame(minWidth: 760, minHeight: 480)
        }
    }
}
